services:
  - type: web
    name: nestquest
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./run.sh
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.12.0
